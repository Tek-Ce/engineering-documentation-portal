# backend/app/api/deps.py

## Module docstring

API dependencies for authentication and authorization

## Classes

### ProjectAccessInfo

## Functions

- `def _get_role_value(role)` — Helper function to extract the string value from a role (Enum or string)

- `def require_role(required_role)` — Dependency factory to enforce a specific role

Usage:
    @router.get("/admin-only")
    async def admin_endpoint(
        current_user: User = Depends(require_role(UserRole.ADMIN))
    ):
        ...


---

_Autogenerated: backend/app/api/deps.py_
