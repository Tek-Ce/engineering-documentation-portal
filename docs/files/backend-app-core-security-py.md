# backend/app/core/security.py

## Functions

- `def verify_password(plain_password, hashed_password)` — Verify password against hash

- `def get_password_hash(password)` — Hash a password

- `def create_access_token(data, expires_delta)` — Create JWT token

- `def decode_token(token)` — Decode JWT token


---

_Autogenerated: backend/app/core/security.py_
